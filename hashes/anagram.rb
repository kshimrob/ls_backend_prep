words =  ['demo', 'none', 'tied', 'evil', 'dome', 'mode', 'live',
          'fowl', 'veil', 'wolf', 'diet', 'vile', 'edit', 'tide',
          'flow', 'neon']
# puts words.group_by { |word| word.downcase.chars.sort }.values

###ALTERNATIVE METHOD###

result = {}

words.each do |word|
  key = word.split('').sort.join
  if result.has_key?(key)
    result[key].push(word)
  else
    result[key] = [word]
  end
end

puts result

result.each do |k,v|
  puts "----"
  puts v
end